# Ship the compiled runtime and its assets only. Everything else in the repo is
# development-only: editor/agent config, skill libraries, coverage reports,
# review + security documents, and local logs. Deny everything, then re-include.

**
!package.json
!README.md
!CHANGELOG.md
!LICENSE.md
!out/**
!resources/**
